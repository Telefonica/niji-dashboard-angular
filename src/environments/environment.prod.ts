import { config } from "rxjs";

// TODO: replace for right config, this is only for local performance tests
export const environment = {
    production: true,
    apiUrl: 'http://localhost:5000',
    apiPrefix: '',
    countryCode: 'ES',
    countryName: 'spain',
};
